NODE_ENV=development
PORT=4000
MONGODB_URI=
JWT_SECRET=change_me_dev_secret
CORS_ORIGIN=http://localhost:3000

# Email (use MailHog or similar in dev)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
MAIL_FROM=noreply@example.com

# Disable automatic memory DB fallback unless explicitly desired
FALLBACK_MEMORY_DB=false
USE_MEMORY_DB=false
NODE_ENV=development
PORT=4000
CLIENT_URL=http://localhost:5173
MONGODB_URI=memory
JWT_SECRET=dev_secret_change_me
JWT_EXPIRES_IN=7d
EMAIL_FROM=no-reply@example.com
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
